## Note on LLM fine-tuning

A recurring lesson working on LoRA/QLoRA, adapter merging, and quantization: the failure mode is almost never where the intuition points. Measuring the change end-to-end — not reasoning about it in isolation — is what surfaces the real bottleneck. Documenting it here so the next decision starts from evidence rather than a guess.
